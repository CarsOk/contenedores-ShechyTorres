import 'package:flutter/material.dart';


class Menu4 extends StatelessWidget {
  const Menu4({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Menu 4')),
      body: Center(
        child: Text('Estas en Menu 4'),
      ),
    );
  }
}